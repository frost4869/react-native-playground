import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  errorMessage: {
    textAlign: 'center',
    marginTop: 16,
    color: 'red',
  },
  btnLabel: {
    color: 'black',
  },
});

export default styles;
