import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#000',
    flex: 1,
  },
  header: {
    marginTop: 20,
    marginHorizontal: 16,
  },
});

export default styles;
