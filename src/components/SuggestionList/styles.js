import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    margin: 8,
    backgroundColor: '#fff',
    borderRadius: 4,
    flex: 1,
  },
  description: {
    padding: 8,
    marginBottom: 3,
  },
});

export default styles;
