import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 16,
    marginTop: 8,
    marginHorizontal: 8,
    backgroundColor: '#fff',
    borderRadius: 4,
    color: '#000',
  },
});

export default styles;
